@model ProjectPBF.Models.ForumThreadModel

@{
    var forumId = ViewBag.ForumId as int? ?? Model.ForumId;
}

<h2>Nowy w¹tek</h2>

<form asp-action="Create" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="ForumId" />
    <div class="mb-3">
        <label class="form-label">Tytu³</label>
        <input asp-for="Title" class="form-control" />
    </div>
    <div class="mb-3">
        <label class="form-label">Treœæ pierwszego posta</label>
        <textarea name="initialContent" class="form-control"></textarea>
    </div>
    <button class="btn btn-success" type="submit">Stwórz</button>
    <a class="btn btn-secondary" asp-action="Index" asp-route-forumId="@forumId">Anuluj</a>
</form>